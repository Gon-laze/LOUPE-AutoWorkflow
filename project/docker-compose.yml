version: "3.9"
services:
  autoworkflow:
    build: .
    container_name: autoworkflow
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./runtime:/app/runtime
    restart: unless-stopped
